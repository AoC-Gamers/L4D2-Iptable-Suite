# Firewall Rules (IPTables + NFTables)

# Backend execution:
# - Legacy backend: ./iptables.rules.sh
# - Modern backend: ./nftables.rules.sh
# Both entrypoints use this same .env file and modular loader.

# Optional module allowlist (comma-separated module IDs)
# Works for both backends and is merged with CLI --only.
# If set, only listed modules are executed.
# Examples:
# MODULES_ONLY="ip_chain_setup,ip_l4d2_udp_base,ip_finalize"
# MODULES_ONLY="nf_chain_setup,nf_geo_country_filter,nf_l4d2_udp_base,nf_finalize"
MODULES_ONLY=""

# Type of iptables chain to protect
# 0: INPUT only (native servers), 1: DOCKER-USER only (containers), 2: BOTH
TYPECHAIN=0

# Docker compatibility when running TYPECHAIN=0
# true: preserves Docker forwarding/chains while still applying INPUT protection
# false: keeps full cleanup behavior (can break Docker network chains)
DOCKER_INPUT_COMPAT=false

# Auto-recover Docker chains when using TYPECHAIN=0 + DOCKER_INPUT_COMPAT=true
# true: restart Docker automatically if DOCKER-USER / DOCKER-FORWARD are missing
# false: only warns
DOCKER_CHAIN_AUTORECOVER=true

# HTTP/HTTPS ports to protect on host INPUT chain
# Format: "80,443" or ranges "8080:8090"
HTTP_HTTPS_PORTS="80,443"

# Rate and burst for NEW TCP connections to HTTP/HTTPS
# iptables format: "240/min", "60/sec", "1000/hour"
# nftables backend expects second/minute/hour/day.
HTTP_HTTPS_RATE="240/min"
HTTP_HTTPS_BURST=480

# Log prefix for blocked/abusive HTTP/HTTPS bursts
LOG_PREFIX_HTTP_HTTPS_ABUSE="HTTP_HTTPS_ABUSE: "

# Additional TCP ports for Docker containers (rate-limiting via DOCKER-USER)
# Examples: "80,443" (HTTP/HTTPS), "8080" (HTTP-ALT), "3000:3100" (range)
# Empty: disabled. Only applies when TYPECHAIN includes container path (1 or 2)
TCP_DOCKER=""

# Custom SSH ports for the system
# Format: "22" (standard), "22022,22023,22024" (multiple), "22020:22029" (range)
# These ports are always allowed for remote administration
SSH_PORT="22022,22023"

# SSH ports for Docker containers (optional)
# Allows SSH access to specific containers. Same format as SSH_PORT
# Empty: disabled. Only applies when TYPECHAIN includes container path (1 or 2)
SSH_DOCKER=""

# SSH anti-abuse rate limiting (NEW connections)
# Format examples: "60/min", "10/sec", "600/hour"
SSH_RATE="60/min"
SSH_BURST=20

# Log prefix for blocked SSH abuse bursts
LOG_PREFIX_SSH_ABUSE="SSH_ABUSE: "

# OpenVPN Server adaptation (host or Docker)
# Include module token: openvpn_server
OVPNSRV_PROTO="udp"
OVPNSRV_PORT=1194
OVPNSRV_SUBNET="10.8.0.0/24"
OVPNSRV_INTERFACE="tun0"
OVPNSRV_DOCKER_INTERFACE=""
OVPNSRV_LAN_SUBNET="192.168.1.0/24"
OVPNSRV_LAN_INTERFACE=""
OVPNSRV_ENABLE_NAT=false
OVPNSRV_ROUTER_REAL_IP=""
OVPNSRV_ROUTER_ALIAS_IP=""
OVPNSRV_LOG_ENABLED=false
OVPNSRV_LOG_PREFIX="VPN_SERVER_TRAFFIC: "

# OpenVPN Site-to-Site adaptation
# Include module token: openvpn_sitetosite
# Incompatible with openvpn_server: do not enable both at once.
OVPNS2S_INTERFACE=""
OVPNS2S_LOCAL_SUBNETS=""
OVPNS2S_REMOTE_SUBNETS=""
OVPNS2S_ENABLE_NAT=false
OVPNS2S_LOCAL_INTERFACE=""
OVPNS2S_LOG_ENABLED=false
OVPNS2S_LOG_PREFIX="VPN_S2S_TRAFFIC: "
# Multi-alias format: "real_ip;alias_ip,real_ip;alias_ip"
# Example: OVPNS2S_ROUTER_ALIAS="192.168.1.1;10.99.2.1,192.168.1.254;10.99.2.254"
OVPNS2S_ROUTER_ALIAS=""
# Legacy single-alias format (optional, backwards compatible)
OVPNS2S_ROUTER_REAL_IP=""
OVPNS2S_ROUTER_ALIAS_IP=""
OVPNS2S_ROUTER_ALIAS_SNAT=false

# Extra allowed ports (servicios adicionales)
# Formato: "puerto,puerto" o rangos "inicio:fin" (multiport)
# Vacío = no agrega excepciones extras.
UDP_ALLOW_PORTS=""
TCP_ALLOW_PORTS=""

# Docker DNS egress subnet allowlist (for ACME and container name resolution)
# Allows UDP/TCP 53 from Docker bridge subnets through FORWARD/DOCKER-USER.
# Default Docker bridge range is typically 172.16.0.0/12.
# Comma-separated CIDRs.
DOCKER_DNS_EGRESS_SUBNETS="172.16.0.0/12"

# Docker monitoring egress allowlist (for Prometheus -> node-exporter, etc.)
# Allows TCP traffic from Docker subnets to specific LAN targets/ports.
# - DOCKER_MONITOR_EGRESS_SUBNETS: source Docker CIDRs (comma-separated)
# - DOCKER_MONITOR_EGRESS_TARGETS: destination IPv4 list (comma-separated)
# - DOCKER_MONITOR_EGRESS_TCP_PORTS: port list/range (comma-separated)
DOCKER_MONITOR_EGRESS_SUBNETS="172.16.0.0/12"
DOCKER_MONITOR_EGRESS_TARGETS=""
DOCKER_MONITOR_EGRESS_TCP_PORTS="9100"

# Source Engine game server ports (GameServer)
# These ports handle: player connections, A2S queries, Steam communication
# Standard format: "27001:27016" (16 servers), "27015" (single server)
L4D2_GAMESERVER_UDP_PORTS="27015"

# SourceTV ports (spectators and streaming)
# Separated from GameServer for better control. Only handle spectator traffic
# Standard format: "27101:27116" (matches GameServer count)
L4D2_SOURCETV_UDP_PORTS="27020"

# Explicit TCP protection ports (RCON / exposed TCP endpoints only)
# Leave empty to avoid coupling TCP protection to all GameServer/SourceTV ports.
L4D2_GAMESERVER_TCP_PORTS=""

# Server tickrate (determines cmdrate limits)
# Typical tickrates: 60 (casual), 100 - 128 (competitive)
# Used to calculate UDP limits: L4D2_CMD_LIMIT+10 (burst), L4D2_CMD_LIMIT+30 (maximum)
L4D2_CMD_LIMIT=100

# Base NEW UDP limiter for game discovery/login traffic.
# These values are intentionally looser than the old 1/s profile to avoid
# breaking Source server browser and Steam Group discovery on busy nodes.
UDP_NEW_SRC_RATE=8
UDP_NEW_SRC_BURST=24
UDP_NEW_GLOBAL_RATE=240
UDP_NEW_GLOBAL_BURST=960

# Bypass documented/observed Source signed packets (0xFFFFFFFFxx) before the
# generic NEW limiter, so A2S / Steam legacy / login traffic is classified by
# dedicated modules instead of being dropped too early.
ENABLE_UDP_NEW_FFFFFFFF_BYPASS=true

# Drop oversized NEW UDP packets before the generic limiter.
# Uses total IP packet length, useful against 1200-byte spray floods that do
# not look like Source query/login traffic.
# Keep the threshold comfortably above short-query/login packets.
ENABLE_UDP_NEW_LARGE_FILTER=false
UDP_NEW_LARGE_DROP_MIN_LEN=1024

# Logging policy for the preventive UDP base limiter.
# Keep false in production if you only want logs for abusive traffic.
ENABLE_UDP_BASELINE_LOGS=false

# A2S and Steam Group query limits (more flexible defaults)
# Rate units are per-second integer values.
A2S_INFO_RATE=16
A2S_INFO_BURST=80
A2S_PLAYERS_RATE=12
A2S_PLAYERS_BURST=60
A2S_RULES_RATE=8
A2S_RULES_BURST=40
STEAM_GROUP_RATE=6
STEAM_GROUP_BURST=30

# L4D2 short login query limits (0x71 / connect-reserve)
L4D2_LOGIN_RATE=4
L4D2_LOGIN_BURST=16

# Steam Group / legacy signed query bytes to match (hex byte list without 0x prefix)
# Default: 69. Add 00 only if packet captures show it is needed: "69,00".
ENABLE_STEAM_GROUP_FILTER=true
STEAM_GROUP_SIGNATURES="69"

# Logging policy for packet normalization/sanitization drops.
# Keep false in production if you only want logs for abusive traffic.
ENABLE_PACKET_NORMALIZATION_LOGS=false

# Drop only clearly invalid UDP sizes by default. Set true only if packet
# captures confirm the malformed-size heuristic does not break discovery.
ENABLE_MALFORMED_FILTER=false

# Trusted IPs (complete whitelist)
# These IPs bypass ALL rate-limits and TCP blocks
# Format: "1.2.3.4" (single IP), "1.2.3.4 5.6.7.8" (multiple space-separated)
# Use for: administrators, monitoring servers, absolutely trusted IPs
WHITELISTED_IPS=""

# Trusted domains resolved to IPv4 at apply time (merged with WHITELISTED_IPS)
# Format: "host1.example.com host2.example.com" (space-separated)
# Note: if DNS changes, re-run rules to refresh resolved IPs.
WHITELISTED_DOMAINS=""

# Optional geo policy for game UDP (nftables backend only)
# WHITELISTED_IPS and WHITELISTED_DOMAINS keep full bypass before this policy.
# GEO_POLICY_MODE: off, allowlist, denylist
# GEO_POLICY_ALLOWED_COUNTRIES / GEO_POLICY_DENIED_COUNTRIES use ISO country codes.
# Country files must exist as GEO_POLICY_DATA_DIR/CC.ipv4.txt
GEO_POLICY_MODE=off
GEO_POLICY_ALLOWED_COUNTRIES="CL,AR,PE,CO,MX,BO,US"
GEO_POLICY_DENIED_COUNTRIES="RU,CN"
GEO_POLICY_DATA_DIR="./geoip/generated/countries"
GEO_POLICY_MANUAL_ALLOW_FILE="./geoip/manual/allow_ipv4.txt"
GEO_POLICY_MANUAL_DENY_FILE="./geoip/manual/deny_ipv4.txt"

# Optional metadata for structured FW_EVT prefixes
FIREWALL_HOST_ALIAS=""

# Detailed log prefixes for specific attack types
# These allow for more granular analysis of different attack patterns
LOG_PREFIX_INVALID_SIZE="INVALID_SIZE: "
LOG_PREFIX_MALFORMED="MALFORMED: "
LOG_PREFIX_A2S_INFO="A2S_INFO_FLOOD: "
LOG_PREFIX_A2S_PLAYERS="A2S_PLAYERS_FLOOD: "
LOG_PREFIX_A2S_RULES="A2S_RULES_FLOOD: "
LOG_PREFIX_STEAM_GROUP="STEAM_GROUP_FLOOD: "
LOG_PREFIX_L4D2_CONNECT="L4D2_CONNECT_FLOOD: "
LOG_PREFIX_L4D2_RESERVE="L4D2_RESERVE_FLOOD: "
LOG_PREFIX_UDP_NEW_LIMIT="UDP_NEW_LIMIT: "
LOG_PREFIX_UDP_EST_LIMIT="UDP_EST_LIMIT: "
LOG_PREFIX_TCP_RCON_BLOCK="TCP_RCON_BLOCK: "
LOG_PREFIX_ICMP_FLOOD="ICMP_FLOOD: "

# ============================================================================

# Persistence Notes
# - ipp.sh manages persistence for iptables backend.
# - nftables backend persistence should be managed with native nft/system methods.
#
# If you operate only one backend, keep the same variables and execute only one entrypoint.

# Log Analysis Configuration

# Path to the iptables log file
# This file contains iptables logs generated by the iptables rules
# Default location where iptables writes blocked packet information
# LOGFILE=/var/log/firewall-suite.log
LOGFILE=/var/log/firewall-suite.log

# Path to rsyslog configuration file for firewall logs
# This file configures rsyslog to capture iptables logs
# RSYSLOG_CONF=/etc/rsyslog.d/firewall-suite.conf
RSYSLOG_CONF=/etc/rsyslog.d/firewall-suite.conf

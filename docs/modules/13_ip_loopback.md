# Módulo específico ip — loopback

## Módulo
- `modules/ip/ip_05_loopback.sh` (`ID=ip_loopback`)

## Objetivo
Permitir tráfico local `lo` en backend `iptables`.

## Nota
En `nft`, este comportamiento se integra en `nf_chain_setup`.
